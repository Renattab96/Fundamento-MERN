import React, {Component} from "react";
import styles from '../styles/Navigation.module.css'
class Navigation extends Component{
    render(){
        return(
            <nav className={styles.nav} >navigation de react</nav>
        )
    }
}

export default Navigation