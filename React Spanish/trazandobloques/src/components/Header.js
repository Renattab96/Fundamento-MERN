import React, { Component } from 'react'
import styles from '../styles/Header.module.css'
class Header extends Component{
    render(){
        return(
            <header className={styles.header} >header de react</header>
        )
    }
}

export default Header